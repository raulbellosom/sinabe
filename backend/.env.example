DATABASE_URL="mysql://root:root@localhost:3306/sinabe"
PORT=4000
JWT_SECRET="secret-token"
APP_URL="http://localhost:4000"
SMTP_HOST=smtp.servidor.com
SMTP_PORT=587
SMTP_USER=usuario@servidor.com
SMTP_PASS=contraseña_o_app_password
SMTP_REQUIRE_AUTH=true           # true = usa usuario/contraseña, false = relay sin password
CUSTODY_FROM="Resguardo TI <no-reply@empresa.com>"
CUSTODY_DIST_LIST="inventarios@empresa.com,ti@empresa.com"

# Notificaciones
NOTIFY_FROM="Sinabe <no-reply@empresa.com>"
NOTIFY_DEFAULT_CC=""
NOTIFY_DEFAULT_BCC=""
NOTIFY_TIMEZONE="America/Mexico_City"

# Push Notifications (Web Push / VAPID)
# Generar con: npx web-push generate-vapid-keys
# O llamar POST /api/push/generate-vapid-keys
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:admin@sinabe.com"

# Firebase Cloud Messaging (opcional, para Android/iOS)
# FIREBASE_PROJECT_ID=""
# FIREBASE_PRIVATE_KEY=""
# FIREBASE_CLIENT_EMAIL=""